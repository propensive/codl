tel 1.0 example.org/documents-by-form

# This document carries three Scalar values, each in a different atom
# form (per §14–§15 of the TEL Specification, with selection rules in
# §22.3). The three field names indicate the form chosen; the field
# values are payloads characteristic of each form.

inline-value ipv4-strict

source-value
    { "address": "192.0.2.1",
      "mask":    "255.255.255.0" }

literal-value
      ---
# this would be a comment without the literal-atom wrapping
## subheading
the value contains a sigil at the start of a line
      ---
